import { PlaceIconsDirective } from './place-icons.directive';

describe('PlaceIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceIconsDirective();
    expect(directive).toBeTruthy();
  });
});
